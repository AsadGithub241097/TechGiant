/** Idle timeout before automatic logout (30 minutes). */
export const SESSION_IDLE_TIMEOUT_MS = 30 * 60 * 1000;

/** Maximum session length regardless of activity (8 hours). */
export const SESSION_MAX_DURATION_MS = 8 * 60 * 60 * 1000;
